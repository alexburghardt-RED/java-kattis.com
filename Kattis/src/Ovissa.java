import java.util.Scanner;
public class Ovissa {
    //Anzahl u in Integer Wert ausgeben
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String sound = scanner.nextLine();
        int u = sound.length();
        System.out.println(u);
            }
        }